rustfmt
define a simple env
start bootstrapping parsing
hack on path/env till you can run ls
play with no-copy on &str instead of String

figure out if you want to always start by unpacking something that totally parses it, or repeatedly parse (I think the former...)
